media:
  input: "images"
  output: "images"

content:
  # --- ГЛАВНАЯ СТРАНИЦА ---
  - name: homepage
    label: "Главная страница"
    type: file
    path: "index.html"
    fields:
      - name: body
        label: "HTML-код страницы"
        type: code

  # --- ОБО МНЕ ---
  - name: about
    label: "Обо мне"
    type: file
    path: "about.html"
    fields:
      - name: body
        label: "HTML-код страницы"
        type: code

  # --- УСЛУГИ ---
  - name: services
    label: "Услуги"
    type: file
    path: "services.html"
    fields:
      - name: body
        label: "HTML-код страницы"
        type: code

  # --- БЛОГ (Статьи) ---
  - name: blog
    label: "Блог"
    type: collection
    path: "blog"
    view:
      fields: [title, date]
    fields:
      - name: title
        label: "Заголовок статьи"
        type: string
      - name: date
        label: "Дата"
        type: date
      - name: body
        label: "Текст статьи (Markdown/HTML)"
        type: rich-text
